<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Car Audio</title>
    <style>
      body {
        font-family: Arial, Helvetica, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f5f5f5;
      }

      .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 20px;
      }

      h1 {
        text-align: center;
        margin-top: 50px;
      }

      form {
        background-color: white;
        margin-top: 20px;
        box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1);
        border-radius: 4px;
        padding: 10px;
      }

      form label {
        font-size: 18px;
        font-weight: 700;
        color: #333;
        display: block;
        margin-bottom: 10px;
      }

      form input[type="text"],
      form input[type="number"] {
        width: 100%;
        padding: 10px;
        font-size: 16px;
        border: 1px solid #ddd;
        border-radius: 4px;
        margin-bottom: 20px;
      }

      form button {
        background-color: #007bff;
        color: white;
        font-size: 16px;
        font-weight: 700;
        border: none;
        border-radius: 4px;
        padding: 10px 20px;
        cursor: pointer;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <h1>Edit Car Audio</h1>
      <form method="POST" action="/car_audio/<%= car_audio.id %>?_method=PUT">
        <label for="name">Name</label>
        <input type="text" id="name" name="name" value="<%= car_audio.name %>" />

        <label for="description">Description</label>
        <input type="text" id="description" name="description" value="<%= car_audio.description %>" />

        <label for="image_url">Image URL</label>
        <input type="text" id="image_url" name="image_url" value="<%= car_audio.image_url %>" />

        <label for="brand">Brand</label>
        <input type="text" id="brand" name="brand" value="<%= car_audio.brand %>" />

        <label for="power">Power (W)</label>
        <input type="number" id="power" name="power" value="<%= car_audio.power %>" />

        <label for="speakers">Number of Speakers</label>
        <input type="number" id="speakers" name="speakers" value="<%= car_audio.speakers %>" />

        <label for="subwoofers">Number of Subwoofers</label>
        <input type="number" id="subwoofers" name="subwoofers" value="<%= car_audio.subwoofers %>">

        <label for="amplifier">Amplifier</label>
        <input type="text" id="amplifier" name="amplifier" value="<%= car_audio.amplifier %>" />

        <label for="price">Price</label>
        <input type="text" id="price" name="price" value="<%= car_audio.price %>" />

        <button type="submit">Save Changes</button>
    </form>
  </div>
</body>
</html>
