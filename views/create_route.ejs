<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
<body>
  <h1>Create a new route</h1>
  <form method="POST" action="/create_route">
  <label for="departure_station">Departure Station:</label>
  <select name="departure_station">
    <% for (var i = 0; i < stations.length; i++) { %>
      <option value="<%= stations[i].idStations %>"><%= stations[i].city %> (<%= stations[i].name %>)</option>
    <% } %>
  </select>
<br>
  <label for="arrival_station">Arrival Station:</label>
  <select name="arrival_station">
    <% for (var i = 0; i < stations.length; i++) { %>
      <option value="<%= stations[i].idStations %>"><%= stations[i].city %> (<%= stations[i].name %>)</option>
    <% } %>
  </select>
    <br>
    <label for="departure_date">Departure Date:</label>
    <input type="date" name="departure_date" min="<%= new Date().toISOString().split('T')[0] %>" required>
    <br>
    <label for="departure_time">Departure Time:</label>
    <input type="time" name="departure_time" required>
    <br>
    <label for="price">Price:</label>
    <input type="number" name="price" step="50" min="0" required>
    <br>
    <button type="submit">Create Route</button>
  </form>
</body>
</html>
